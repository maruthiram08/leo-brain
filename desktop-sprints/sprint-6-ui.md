# Leo Desktop - Sprint 6: UI Polish

## Task
Replace the default "Hello World" renderer with a modern, premium React UI. This window will serve as the "Settings" or "Status" dashboard for the app.

## Completion Criteria
- [ ] Install `react`, `react-dom`, `@types/react`, `@types/react-dom`.
- [ ] Update `vite.renderer.config.ts` to support React (if not already handled by plugin).
- [ ] Create `src/renderer.tsx` (entry point).
- [ ] Build a `App.tsx` component with:
    - [ ] Connectivity Status (Checking `electron-store` or via IPC).
    - [ ] "Log In / Log Out" buttons.
    - [ ] A clean, dark-mode, minimal aesthetic (Black/White/Inter font).
- [ ] Verify the UI interactions (IPC between Renderer and Main).

## Max Iterations
10
